# Copy to .env next to docker-compose.yml — Compose loads this file automatically
# for ${VAR} substitution. Use mongodb://mongodb:27017/ here (service name on the
# compose network), not 127.0.0.1.

MONGO_URI=mongodb://mongodb:27017/
SECRET_KEY=dev
WEB_PORT=3000
ML_PORT=5001
MONGO_HOST_PORT=27017
